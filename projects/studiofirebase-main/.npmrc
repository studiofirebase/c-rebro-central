# Allow installs even when upstream packages have outdated peerDependencies.
# This repo currently uses firebase-admin v13.x, but @next-auth/firebase-adapter
# still declares a peerDependency on firebase-admin ^11.4.1.
legacy-peer-deps=true
