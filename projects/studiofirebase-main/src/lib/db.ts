/**
 * Prisma foi removido do projeto.
 *
 * Este arquivo permanece apenas para compatibilidade com imports antigos.
 * Não use `prisma` — utilize Firebase Admin / Firestore via `src/lib/firebase-admin.ts`.
 */

export const prisma = undefined as never;
